// Client-side GIF player that draws frames to a canvas and can skip near-empty frames.
// Uses dynamic import of gifuct-js so build doesn't fail if not installed.
// GIF player removed — stub to satisfy any remaining imports.
export async function playGifOnCanvas(_: HTMLCanvasElement, __: string, ___?: any) {
  return { stop() {} };
}
